package rit.cs;

public interface Expression {
    /**
     * creates the interface for
     *
     * @evaluate will be defined later
     *
     * @emit  will be defined later
     */
    int evaluate();
    String emit();
}
